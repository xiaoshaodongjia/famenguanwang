# 阀门销售官网 — 实施规划方案

## Context

需要为阀门销售业务搭建一个全栈展示网站。核心需求：
- **前台展示**：阀门产品列表、详情页，展示图片、价格、全部参数（口径、压力、材质、连接方式、型号、品牌、适用介质、温度范围、尺寸）
- **后台管理**：管理员登录后可增删改查产品、上传图片和参数
- **全端响应式**：同时适配桌面、平板、手机

当前项目目录为空，从零开始搭建。

---

## 推荐技术栈

| 层次 | 技术 | 理由 |
|------|------|------|
| 框架 | **Nuxt.js 3** | Vue 的全栈框架，前后端一体，API 路由内置 |
| 语言 | **TypeScript** | 阀门参数结构复杂，类型安全有价值 |
| 样式 | **Tailwind CSS** | 快速实现响应式设计 |
| ORM | **Prisma** | 简洁的 schema 定义，SQLite 零配置 |
| 数据库 | **SQLite** | 单文件数据库，无需服务器，适合小规模 |
| 认证 | **Nuxt Auth (自定义 session)** | 单管理员账号密码登录，轻量方案 |
| 图片存储 | **本地 /public/uploads** | 零配置，后续可迁移到云存储 |
| 部署 | **Vercel / 阿里云 ECS / 任意 Node 服务器** | Nuxt 原生支持 |

---

## 项目结构

```
阀门销售官网/
├── prisma/
│   ├── schema.prisma          # 数据库模型定义
│   └── seed.ts                # 示例阀门数据
├── public/
│   └── uploads/               # 产品图片存储
├── server/
│   ├── api/
│   │   ├── auth/
│   │   │   ├── login.post.ts  # 登录接口
│   │   │   └── logout.post.ts # 登出接口
│   │   ├── products/
│   │   │   ├── index.get.ts   # 产品列表
│   │   │   ├── index.post.ts  # 创建产品
│   │   │   └── [id].get.ts    # 产品详情
│   │   │   └── [id].put.ts    # 更新产品
│   │   │   └── [id].delete.ts # 删除产品
│   │   └── upload.post.ts     # 图片上传
│   ├── middleware/
│   │   └── auth.ts            # 登录校验中间件
│   └── utils/
│       └── session.ts         # Session 管理
├── app/
│   ├── app.vue                # 根组件
│   ├── pages/
│   │   ├── index.vue          # 首页
│   │   ├── products/
│   │   │   ├── index.vue      # 产品列表页
│   │   │   └── [id].vue       # 产品详情页
│   │   └── admin/
│   │       ├── index.vue      # 管理后台首页
│   │       ├── login.vue      # 登录页
│   │       └── products/
│   │           ├── index.vue  # 产品管理列表
│   │           └── [id].vue   # 编辑产品
│   ├── components/
│   │   ├── ProductCard.vue    # 产品卡片
│   │   ├── ProductGrid.vue    # 产品网格
│   │   ├── ProductParams.vue  # 阀门参数展示表
│   │   └── ProductForm.vue    # 产品表单（后台用）
│   └── composables/
│       └── useAuth.ts         # 登录状态管理
├── nuxt.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── .env                       # 环境变量
```

---

## 数据库 Schema

```prisma
model Product {
  id            Int      @id @default(autoincrement())
  name          String                  // 产品名称
  model         String                  // 型号
  brand         String                  // 品牌
  category      String                  // 类别（球阀/闸阀/截止阀等）
  price         Float?                  // 价格（可为空=询价）
  dn            String                  // 口径（DN15/DN25...）
  pn            String                  // 压力等级（PN16/PN25...）
  material      String                  // 材质
  connection    String                  // 连接方式（法兰/螺纹/焊接）
  media         String                  // 适用介质
  temperature   String                  // 温度范围
  dimensions    String?                 // 尺寸（JSON字符串）
  image         String?                 // 主图路径
  description   String?                 // 详细描述
  isActive      Boolean  @default(true) // 上架/下架
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Admin {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String   // bcrypt 加密
  createdAt DateTime @default(now())
}
```

---

## API 设计

### 公开接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/products` | 产品列表（支持分类筛选、搜索、分页） |
| GET | `/api/products/[id]` | 产品详情 |

### 管理接口（需登录）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/products` | 创建产品 |
| PUT | `/api/products/[id]` | 更新产品 |
| DELETE | `/api/products/[id]` | 删除产品 |
| POST | `/api/upload` | 上传图片 |

---

## 页面结构

### 前台页面
- **首页 `/`**：公司简介、推荐产品、分类导航
- **产品中心 `/products`**：筛选侧边栏 + 产品网格 + 搜索
- **产品详情 `/products/[id]`**: 图片、价格、完整参数表

### 后台页面
- **登录 `/admin/login`**: 账号密码登录
- **产品管理 `/admin/products`**: 产品列表表格 + 增删改操作 + 图片上传

---

## 实施阶段

### Phase 1：项目初始化
- 初始化 Nuxt 3 + TypeScript + Tailwind 项目
- 配置 Prisma + SQLite
- 定义数据库 Schema 并执行迁移
- 创建根布局和基础路由

### Phase 2：数据库与 API
- 实现公开 API（产品列表、详情）
- 实现管理 API（CRUD、图片上传）
- 编写种子数据脚本（5-10个示例阀门产品）

### Phase 3：前台页面
- 首页布局与样式
- 产品列表页（筛选、搜索、分页）
- 产品详情页（参数表格展示）
- 响应式适配

### Phase 4：后台管理
- Session 认证 + 登录页 + 中间件守卫
- 产品管理列表页
- 添加/编辑产品表单（含图片上传）
- 删除功能

### Phase 5：完善与部署
- 加载状态、错误处理、空状态
- SEO 优化（meta 信息）
- 部署上线

---

## 验证方式

1. `npm run dev` 启动开发服务器
2. 访问 `http://localhost:3000` 查看首页和产品列表
3. 点击产品进入详情页，确认参数完整展示
4. 访问 `/admin/login` 登录后台
5. 测试添加产品（含图片上传）、编辑、删除
6. 用浏览器开发者工具切换设备模式，验证响应式布局

---

## 前端技术说明（Vue / Nuxt）

| 概念 | 说明 |
|------|------|
| **Vue 3 Composition API** | 用 `<script setup>` 语法写组件，响应式数据用 `ref` / `reactive` |
| **Nuxt 页面路由** | `pages/` 目录下的 `.vue` 文件自动对应路由，如 `products/[id].vue` |
| **Nuxt API 路由** | `server/api/` 下的文件自动成为后端接口 |
| **Composables** | 可复用的逻辑函数（如 `useAuth` 管理登录状态） |
| **Tailwind CSS** | 直接在 class 里写样式，如 `class="grid grid-cols-1 md:grid-cols-3"` |

Vue 相比 React 对新手更友好：模板语法接近 HTML，单文件组件（`.vue`）把 HTML/CSS/JS 写在一起，结构直观。
