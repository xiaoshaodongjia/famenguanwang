# 阀门销售官网 — 运维手册

> 服务器：阿里云 ECS `60.205.8.208`  
> 系统：Alibaba Cloud Linux 3  
> 部署路径：`/var/www/valve-website`

---

## 一、常用运维命令

### PM2 进程管理

```bash
# 查看进程状态
pm2 status

# 查看实时日志
pm2 logs valve-website

# 查看最近 N 行日志
pm2 logs valve-website --lines 20

# 重启应用
pm2 restart valve-website

# 停止应用
pm2 stop valve-website

# 启动应用
pm2 start valve-website

# 删除进程
pm2 delete valve-website
```

### Nginx

```bash
# 测试配置文件语法
nginx -t

# 重启 Nginx
systemctl restart nginx

# 重新加载配置（不断开连接）
systemctl reload nginx

# 查看状态
systemctl status nginx
```

### 查看日志

```bash
# PM2 输出日志
cat /root/.pm2/logs/valve-website-out.log

# PM2 错误日志
cat /root/.pm2/logs/valve-website-error.log

# Nginx 访问日志
tail -f /var/log/nginx/access.log

# Nginx 错误日志
tail -f /var/log/nginx/error.log
```

---

## 二、重新部署（代码更新后）

```bash
# 1. 进入项目目录
cd /var/www/valve-website

# 2. 拉取最新代码（如果用 git）
git pull

# 3. 安装依赖（如有新增）
npm install

# 4. 构建
npm run build

# 5. 重启 PM2
pm2 restart valve-website
```

> ⚠️ **重要**：PM2 使用 `ecosystem.config.cjs` 启动，环境变量（DATABASE_URL 等）配置在其中。  
> 修改环境变量后执行 `pm2 reload ecosystem.config.cjs` 生效。

---

## 三、数据库操作

```bash
cd /var/www/valve-website

# 重置数据库（会清空数据）
npx prisma migrate reset

# 填充种子数据
npm run seed

# 打开 Prisma Studio（可视化编辑）
npx prisma studio
```

---

## 四、服务器信息

| 项目 | 详情 |
|------|------|
| 公网 IP | `60.205.8.208` |
| 网站地址 | http://60.205.8.208 |
| SSH 密钥 | `阀门企业官网.pem` |
| 项目路径 | `/var/www/valve-website` |
| 数据库 | SQLite (`prisma/dev.db`) |
| Node.js | v20.20.2 |
| 管理员账号 | `admin` |
| 管理员密码 | `admin123` |

---

## 五、安全组端口（阿里云控制台配置）

| 端口 | 协议 | 用途 |
|------|------|------|
| 22 | TCP | SSH 远程登录 |
| 80 | HTTP | 网站访问 |
| 443 | HTTPS | 加密访问（后续配置） |

---

## 六、PM2 环境变量配置

项目使用 `ecosystem.config.cjs` 管理环境变量。配置文件内容：

```javascript
module.exports = {
  apps: [{
    name: 'valve-website',
    script: '.output/server/index.mjs',
    cwd: '/var/www/valve-website',        // ⚠️ 必须指定工作目录
    instances: 'max',
    exec_mode: 'cluster',
    env: {
      DATABASE_URL: 'file:/var/www/valve-website/prisma/dev.db',  // ⚠️ 必须用绝对路径
      SESSION_SECRET: 'valve-website-production-secret-2024',
      ADMIN_USERNAME: 'admin',
      ADMIN_PASSWORD: 'admin123',
      NITRO_PORT: 3000
    }
  }]
}
```

> ⚠️ **踩坑记录**：
> - `DATABASE_URL` 必须使用**绝对路径**，否则 PM2 会在错误位置创建空数据库
> - `cwd` 必须指定，否则相对路径从 PM2 根目录（/root）开始解析

---

## 七、故障排查

| 现象 | 排查步骤 |
|------|----------|
| 网站打不开 | 1. `pm2 status` 检查进程<br>2. `systemctl status nginx` 检查 Nginx<br>3. 检查阿里云安全组是否放行 80 端口 |
| 502 Bad Gateway | PM2 进程挂了 → `pm2 restart valve-website` |
| 页面白屏 | 1. 查看 PM2 错误日志<br>2. 检查构建是否成功 → `npm run build` |
| 数据库报错 `table does not exist` | DATABASE_URL 路径错误 → 检查是否用了绝对路径 |
| 登录 500 错误 | 1. `pm2 logs valve-website` 查看错误<br>2. 确认数据库文件存在且有数据<br>3. 检查环境变量配置 |
| 环境变量不生效 | 修改 `ecosystem.config.cjs` 后执行 `pm2 reload ecosystem.config.cjs` |
